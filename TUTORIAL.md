# 详细使用教程

本教程将手把手带你从零开始使用 e卷通自动阅卷工具，**即使你完全不懂编程也能跟着做**。

> 📌 本工具针对历史/政治等主观题阅卷设计，原版示例为某次考试第14题（3 小题，共 14 分）。  
> 教程末尾会教你**如何让 AI 帮你生成任意新题目的评分规则**，无需手动编写代码。

---

## 目录

1. [环境准备](#1-环境准备)
2. [安装 Python 依赖](#2-安装-python-依赖)
3. [安装 ChromeDriver](#3-安装-chromedriver)
4. [申请百度 OCR API（强烈推荐）](#4-申请百度-ocr-api强烈推荐)
5. [配置脚本](#5-配置脚本)
6. [首次运行 —— 校准截图区域](#6-首次运行--校准截图区域)
7. [正式使用流程](#7-正式使用流程)
8. [🤖 用 AI 生成新题目的评分规则](#8--用-ai-生成新题目的评分规则核心)
9. [常见问题排查](#9-常见问题排查)
10. [进阶技巧](#10-进阶技巧)

---

## 1. 环境准备

### 1.1 操作系统要求

- Windows 10 / 11（脚本中的弹窗等待键盘事件使用了 `msvcrt`，仅支持 Windows）
- 已安装 Chrome 浏览器（任何近 2 年的版本均可）

### 1.2 安装 Python

1. 访问 https://www.python.org/downloads/
2. 下载 **Python 3.10 或更高版本**（推荐 3.11）
3. 安装时**务必勾选** ☑️ `Add Python to PATH`
4. 安装完成后，按 `Win + R`，输入 `cmd`，敲回车打开命令行
5. 输入以下命令验证：
   ```bash
   python --version
   ```
   能看到 `Python 3.11.x` 之类的输出就成功了。

---

## 2. 安装 Python 依赖

打开命令行（`Win + R` → `cmd`），逐条执行：

```bash
pip install selenium pillow opencv-python requests
```

如果你想使用本地 OCR（备用方案，无需注册百度账号），再装：

```bash
pip install paddlepaddle paddleocr
```

> ⚠️ PaddleOCR 体积较大（约 500MB），首次运行会下载模型文件，对手写体识别率有限。**强烈建议使用百度 API 方案**。

如果 pip 下载很慢，使用国内镜像：

```bash
pip install selenium pillow opencv-python requests -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 3. 安装 ChromeDriver

ChromeDriver 是 Selenium 控制 Chrome 浏览器的必备组件。

### 3.1 查看你的 Chrome 版本

打开 Chrome，在地址栏输入：

```
chrome://version
```

第一行类似 `Chrome 120.0.6099.130`，**记住前 3 段数字**（如 `120.0.6099`）。

### 3.2 下载对应版本的 ChromeDriver

- Chrome 115 及以下：https://chromedriver.chromium.org/downloads
- Chrome 116 及以上：https://googlechromelabs.github.io/chrome-for-testing/

下载 `chromedriver-win64.zip`，解压得到 `chromedriver.exe`。

### 3.3 放到 PATH 路径中

最简单的方法：把 `chromedriver.exe` 直接放到 **Python 安装目录** 下（与 `python.exe` 同一文件夹）。

或者放到任意目录，然后把该目录加入系统环境变量 `PATH`。

### 3.4 验证

命令行输入：

```bash
chromedriver --version
```

能看到版本号就成功了。

---

## 4. 申请百度 OCR API（强烈推荐）

百度的**手写文字识别**接口对中文手写体识别率最高，**每天免费 500 次**，完全够日常阅卷使用。

### 4.1 注册并实名认证

1. 访问 https://console.bce.baidu.com/
2. 用手机号或微信注册百度智能云账号
3. 完成**个人实名认证**（必需，否则无法调用 API）

### 4.2 开通文字识别服务

1. 登录后，在左侧菜单找到「**人工智能**」→「**文字识别**」
2. 点击「**立即使用**」
3. 在「**概览**」页面，**领取免费资源**（手写文字识别 → 领取每日 500 次免费额度）

### 4.3 创建应用获取密钥

1. 文字识别控制台 → 左侧「**应用列表**」
2. 点击「**创建应用**」
3. 应用名称随意填（如 `e卷通阅卷`）
4. 接口选择：勾选「**手写文字识别**」「**通用文字识别（标准版）**」「**通用文字识别（高精度版）**」
5. 创建完成后，应用列表会显示：
   - **API Key**：一串 24 位字符
   - **Secret Key**：一串 32 位字符
6. **复制保存好这两串密钥**，下一步要用

---

## 5. 配置脚本

### 5.1 下载项目

```bash
git clone https://github.com/lnnnnne/ejt-auto-grader.git
cd ejt-auto-grader
```

或者直接在 GitHub 页面点 `Code` → `Download ZIP` 解压。

### 5.2 修改 `auto_grade_q14.py` 中的配置

用记事本或 VSCode 打开 `auto_grade_q14.py`，找到顶部的「**可调配置区**」：

#### ① 填入百度 API 密钥（第 65、66 行）

```python
BAIDU_API_KEY    = "把你的 API Key 粘贴在这里"
BAIDU_SECRET_KEY = "把你的 Secret Key 粘贴在这里"
```

#### ② 修改截图保存目录（第 37 行）

```python
OUTPUT_DIR = r"C:\Users\你的用户名\Desktop\screenshots"
```

> 💡 `r"..."` 中的 `r` 必须保留，表示不转义反斜杠

#### ③ 其他参数可暂时保持默认

- `PAGE_URL`：e卷通的考试入口，默认 `https://ejt.xkw.com/exam/index`
- `LOGIN_WAIT_SECONDS = 60`：留 60 秒登录时间，按回车可提前跳过
- `NEXT_QUESTION_WAIT = 5`：每题间隔 5 秒，给网页加载时间

---

## 6. 首次运行 —— 校准截图区域

这一步**非常关键**。每个学校的答题卡布局可能不同，需要校准答案区域的坐标。

### 6.1 试运行

```bash
python auto_grade_q14.py
```

脚本会：
1. 自动打开 Chrome 并跳转到 e卷通
2. 命令行显示「等待 60 秒，按回车可提前跳过」
3. 你需要在 Chrome 中**手动登录**并**进入到第 14 题的批阅页面**
4. 60 秒后（或按回车后）脚本会自动截图

### 6.2 查看截图

打开 `OUTPUT_DIR` 目录（你在第 5.2 步设置的截图保存目录），会看到 `raw_fullpage.png`。

### 6.3 用画图工具量出答案区域坐标

1. 用 Windows **画图** 打开 `raw_fullpage.png`
2. 鼠标移动到答案区域**左上角**，看左下角状态栏的像素坐标，记为 `(left, top)`
3. 鼠标移动到答案区域**右下角**，记为 `(right, bottom)`

### 6.4 修改 CROP_BOX

回到 `auto_grade_q14.py`，找到第 56 行：

```python
CROP_BOX = (176, 191, 1766, 1293)
```

改成你量出的坐标：

```python
CROP_BOX = (你的left, 你的top, 你的right, 你的bottom)
```

### 6.5 验证

重新运行脚本，查看生成的 `q14_cropped.png`。如果裁剪后正好是答案区域，校准完成 ✅

> 💡 如果某次答题卡布局有较大变化，重新校准一次即可。

---

## 7. 正式使用流程

校准完成后，正式阅卷流程如下：

### 7.1 启动脚本

```bash
python auto_grade_q14.py
```

### 7.2 登录并定位

- 在弹出的 Chrome 中登录 e卷通教师账号
- 进入「我的任务」→ 选择对应的阅卷任务
- 翻到**第一份**学生答卷的**第 14 题**
- 按回车跳过等待

### 7.3 自动评分循环

脚本会自动执行：

```
截图 → 预处理 → OCR 识别 → 评分 → 弹窗显示结果
   ↑                                          ↓
   └─────── 关闭弹窗后处理下一份 ←─────────────┘
```

### 7.4 弹窗操作说明

弹窗包含：
- **顶部蓝色区域**：显示推荐总分（如 `推荐总分 12 / 14`）
- **中部分值选择区**：每道小题列出 0~满分的按钮，**蓝色高亮**是推荐分
  - 点击其他数字按钮可调整分值
  - 总分会实时更新
- **下部详情区**：显示命中/漏掉的得分点，以及 OCR 识别原文（便于核对）
- **底部绿色「提交」按钮**：点击后自动在网页填分并切换下一题

### 7.5 退出

在命令行按 `Ctrl + C` 终止脚本。

---

## 8. 🤖 用 AI 生成新题目的评分规则（核心）

本工具的精髓在于评分规则可以**任意替换**。每次考试题目不同，你只需要让 AI 根据标准答案生成新的 `SCORING_RULES` 字典即可。

### 8.1 准备工作

收集以下材料：
1. **题目原文**（题干）
2. **标准答案**（参考答案）
3. **评分细则**（每个得分点的分值，例如「科举制 2 分，影响每点 1 分」）

### 8.2 推荐使用的 AI

- Claude（推荐，对复杂结构化输出最稳定）
- ChatGPT (GPT-4)
- 文心一言、通义千问、DeepSeek 等中文模型

### 8.3 🎯 万能提示词模板（直接复制使用）

把下面这段完整复制给 AI，**只需要把【】中的内容替换成你的题目即可**：

````
你是一个 Python 代码生成助手。我有一个自动阅卷脚本，需要你根据标准答案生成 Python 格式的评分规则字典。

# 任务背景
我正在为一份历史/政治主观题编写自动评分规则。学生答案会通过 OCR 识别为文本，再通过关键词匹配自动给分。

# 题目原文
【这里粘贴题目原文，例如：
（1）根据材料一并结合所学知识，指出隋唐时期选官制度的变化及其政治影响。（6分）
（2）根据材料二，概括唐太宗的治国理念，并指出其开创的盛世局面。（4分）
（3）根据材料三，列举宋太祖加强中央集权的具体措施。（4分）】

# 标准答案
【这里粘贴标准答案，例如：
（1）变化：实行科举制（2分）
    政治影响：扩大了统治基础；旧贵族逐渐消失；皇权得到加强（每点2分，最多4分）
（2）重用人才（1分）；善于纳谏（1分）；开创了贞观之治（2分）
（3）解除禁军高级将领的兵权；任用文臣管理军务；调换军队将领定期换防；
    派文臣担任州县长官；设置通判分知州权力；取消节度使收税权
    （每点1分，最多4分）】

# 你需要输出的格式
严格按照以下 Python 字典格式输出，**只输出代码，不要任何解释**：

```python
SCORING_RULES = {
    "q1": {
        "max": <第1题总分>,
        # 如果第1题有固定得分点（如"科举制"），用 keju 字段
        "keju": {
            "name": "<得分点名称>",
            "points": <分值>,
            "keywords": [
                # 每个内层 list 是一组同义词，多组之间是 AND 关系
                # 即学生答案必须每组都至少命中一个词才算得分
                ["关键词A1", "关键词A2", "同义表达A3"],
            ],
        },
        # 如果有多个阶梯式得分点（如"影响"3 点取 2），用 influence_points
        "influence_points": [
            {
                "name": "<影响点1名称>",
                "keywords": [
                    ["动词1组：扩大", "拓宽", "增加"],
                    ["对象1组：统治基础", "社会基础"],
                ],
            },
            # ... 更多影响点
        ],
        # 阶梯分映射：命中数 -> 得分
        "influence_ladder": {0: 0, 1: 2, 2: 4, 3: 4},
    },
    "q2": {
        "max": <第2题总分>,
        "points": [
            {
                "name": "<得分点1名称>",
                "points": <分值>,
                "keywords": [
                    ["近义词1", "近义词2", "近义词3"],
                ],
            },
            # ... 更多得分点
        ],
    },
    "q3": {
        "max": <第3题总分>,
        "measures": [
            {
                "name": "<措施1的标准表述>",
                # 精确关键词：明确对应标准答案的表达
                "exact_keywords": [
                    ["动词组：解除", "收回", "剥夺"],
                    ["对象组：禁军", "兵权", "将领"],
                ],
                # 模糊关键词：方向正确但表述笼统（如古文原文）
                "fuzzy_keywords": [
                    ["收其精兵", "收兵权"],
                ],
            },
            # ... 更多措施
        ],
    },
}
```

# 关键词设计要求（非常重要）
1. **每个关键词组内放同义词**：学生可能用不同词表达同一意思，要尽量覆盖
   - 例如"扩大"可能写成：扩大、拓宽、拓展、扩展、广泛、增加
2. **多个关键词组之间是 AND 关系**：必须每组都命中才算得分
   - 例如"扩大统治基础"需要 [扩大类词] + [统治基础类词] 同时命中
3. **考虑古文/文言原文**：历史题学生常用原文表述（如"收其精兵""制其钱谷"），要放在 fuzzy_keywords
4. **考虑书写错误和简写**：例如"贞观之治"也可能只写"贞观"
5. **第3题（如果是列举型）使用 measures 结构**：每条措施都有 exact_keywords（标准表述）和 fuzzy_keywords（古文/笼统表述）

# 评分逻辑参考（第3题的计分规则已在脚本中固定，无需在 JSON 中体现）
- 精确命中 ≥ 2 条：满分
- 精确命中 1 条：得 50% 分
- 无精确命中但模糊命中 ≥ 2 条：得 50% 分（概括型答案上限）
- 模糊命中 1 条：得 25% 分

请现在生成对应的 SCORING_RULES。
````

### 8.4 使用示例

#### 示例输入（你给 AI 的内容）

```
# 题目原文
（1）根据材料，概括明朝加强君主专制的措施。（6分）

# 标准答案
（1）废除丞相制度（2分）
    设立内阁（2分）
    设立特务机构锦衣卫和东厂（2分）
```

#### 示例输出（AI 生成的代码）

```python
SCORING_RULES = {
    "q1": {
        "max": 6,
        "points": [
            {
                "name": "废除丞相",
                "points": 2,
                "keywords": [
                    ["废除", "废", "取消", "撤销", "罢"],
                    ["丞相", "宰相", "相", "丞相制度", "宰相制度"],
                ],
            },
            {
                "name": "设立内阁",
                "points": 2,
                "keywords": [
                    ["设立", "设", "建立", "创立", "成立", "增设"],
                    ["内阁", "内阁制度"],
                ],
            },
            {
                "name": "锦衣卫和东厂",
                "points": 2,
                "keywords": [
                    ["锦衣卫", "东厂", "西厂", "特务机构", "厂卫"],
                ],
            },
        ],
    },
}
```

### 8.5 替换到脚本中

1. AI 返回代码后，**复制整段 `SCORING_RULES = {...}`**
2. 打开 `auto_grade_q14.py`
3. 找到第 78 行附近的 `SCORING_RULES = {`
4. 把旧的 `SCORING_RULES` 整段替换为 AI 生成的版本
5. **同时也要修改弹窗中的分值上限**（第 703-705 行）：

   ```python
   make_score_row(sel_frame, "第(1)题  /6:", q1_var, 6, 0)  # 改成你的题目分值
   make_score_row(sel_frame, "第(2)题  /4:", q2_var, 4, 1)
   make_score_row(sel_frame, "第(3)题  /4:", q3_var, 4, 2)
   ```

6. 保存 → 重新运行脚本

### 8.6 评分规则验证

修改后强烈建议先**离线测试**：

```bash
python grade_q14.py
```

在交互模式下粘贴几份学生答案样本，看评分是否合理。如果某些得分点漏判，回去补充关键词组。

### 8.7 让 AI 帮你优化关键词

如果发现某些学生答案明明意思对，但没被识别到，可以让 AI 帮你扩展：

````
我的评分规则中"重用人才"得分点的关键词组是：
["重用", "任用", "选用", "重视", "广纳", "招揽"]

但有学生写"礼贤下士""唯才是举""任人唯贤"也没被识别。
请帮我补全这一组的同义词，覆盖古今所有可能表达。
````

---

## 9. 常见问题排查

### Q1：脚本启动后 Chrome 没有打开

**原因**：ChromeDriver 版本不匹配  
**解决**：重新下载与 Chrome 版本一致的 ChromeDriver（见第 3 步）

### Q2：截图全黑 / 截图错位

**原因**：CROP_BOX 坐标错了 或 e卷通页面布局变了  
**解决**：重新执行第 6 步校准

### Q3：OCR 一直返回空文本

**原因之一**：百度 API 密钥填错  
**检查**：命令行输出是否有 `[OCR] 百度Token失败` 字样  
**解决**：去百度智能云控制台重新复制密钥

**原因之二**：图片超过 4MB  
**解决**：脚本已自动压缩，如仍报错，调低 `BAIDU_MAX_MB = 2.5`

**原因之三**：答案区域确实是空白  
**解决**：手动检查 `q14_cropped.png` 内容

### Q4：评分结果不准确

**原因**：评分规则关键词覆盖不全  
**解决**：
1. 看弹窗里的「OCR 识别原文」核对 OCR 是否正确
2. 如果 OCR 正确但评分不对 → 用第 8.7 节的方法扩展关键词
3. 如果 OCR 错误较多 → 调整预处理参数（提升 `SCALE_FACTOR`）

### Q5：自动填分没反应

**原因**：e卷通的打分输入框 CSS 类名变化  
**解决**：在浏览器按 F12，找到打分输入框的 `class`，添加到 `auto_submit_score()` 函数的 selector 列表里

### Q6：报错 `selenium.common.exceptions.WebDriverException`

**解决**：
```bash
pip install --upgrade selenium
```

### Q7：百度 API 报 `error_code: 18`（QPS 超限）

**解决**：免费版每秒只能调用 2 次，在 `NEXT_QUESTION_WAIT` 加大间隔（如改为 8 秒）

---

## 10. 进阶技巧

### 10.1 连接已打开的 Chrome（避免重复登录）

如果不想每次都重新登录，先用调试模式启动 Chrome：

1. **关闭所有 Chrome 窗口**
2. 命令行执行：
   ```bash
   "C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222 --user-data-dir="C:\chrome-debug"
   ```
3. 在新打开的 Chrome 中登录 e卷通
4. 修改 `auto_grade_q14.py`：
   ```python
   USE_EXISTING_BROWSER = True
   ```
5. 再运行脚本，会直接复用这个 Chrome

### 10.2 多题型批量阅卷

如果一次要阅多个不同题型（如第 14、15、16 题），可以：

1. 为每个题目准备一份独立的脚本（如 `auto_grade_q15.py`）
2. 各自修改 `SCORING_RULES`、`CROP_BOX`、分值上限
3. 阅卷时切换运行不同脚本

### 10.3 导出阅卷记录

在 `auto_grade_q14.py` 的 `on_submit` 函数中添加日志：

```python
import csv, datetime
with open("grading_log.csv", "a", encoding="utf-8-sig", newline="") as f:
    writer = csv.writer(f)
    writer.writerow([
        datetime.datetime.now().isoformat(),
        q1, q2, q3, total,
        ocr_text[:200]
    ])
```

每次提交分数会追加一行到 CSV，便于事后核查。

### 10.4 调整 OCR 识别区域分段

如果三道小题的间隔不明显，OCR 区分错乱，可以手动调整 `_split_into_regions` 函数中的阈值：

```python
blank = np.where(row_means > 235)[0]   # 235 是空白阈值，可调高（更严格）
```

---

## 附：完整工作流速查表

| 阶段 | 操作 | 一次性？ |
|------|------|---------|
| 1. 环境 | 装 Python、Chrome、ChromeDriver | 一次性 |
| 2. 依赖 | `pip install selenium pillow opencv-python requests` | 一次性 |
| 3. API | 申请百度 OCR 密钥 | 一次性 |
| 4. 配置 | 填密钥、改路径 | 一次性 |
| 5. 校准 | 试运行 → 量坐标 → 改 CROP_BOX | 每次新题型 |
| 6. **评分规则** | **用 AI 生成 SCORING_RULES → 替换** | **每次新题目** |
| 7. 阅卷 | 启动脚本 → 登录 → 自动循环 | 每次阅卷 |

---

## 反馈与改进

如果你在使用中遇到问题，或想添加新功能，欢迎在 GitHub 仓库提交 Issue：

https://github.com/lnnnnne/ejt-auto-grader/issues
